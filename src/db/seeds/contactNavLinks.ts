/*
import { DB } from "@/db";
import { contactNavLinks } from "@/db/schema";
import { ContactNavLinksSchemaType } from "@/db/schema";

const links: ContactNavLinksSchemaType[] = [
    {
        name: "Contact",
        href: "one",
    },
    {
        name: "Connect",
        href: "two"
    },
    {
        name: "Join",
        href: "three",
    },
    {
        name: "Free",
        href: "four",
    },
    {
        name: "About",
        href: "five",
    },
];

export async function seed(db: DB) {
    await db.insert(contactNavLinks).values(links);
}
*/
