/*
import { DB } from "@/db";
import { lmsNavLinks } from "@/db/schema";
import { LmsNavLinksSchemaType } from "@/db/schema";

const links: LmsNavLinksSchemaType[] = [
    {
        name: "One",
        href: "one",
    },
    {
        name: "Two",
        href: "two"
    },
    {
        name: "Three",
        href: "three",
    },
    {
        name: "Four",
        href: "four",
    },
    {
        name: "Five",
        href: "five",
    },
];

export async function seed(db: DB) {
    await db.insert(lmsNavLinks).values(links);
}
    */
