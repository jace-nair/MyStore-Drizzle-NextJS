/*
import { DB } from "@/db";
import { shopNavLinks } from "@/db/schema";
import { ShopNavLinksSchemaType } from "@/db/schema";

const links: ShopNavLinksSchemaType[] = [
    {
        name: "One",
        href: "one",
    },
    {
        name: "Two",
        href: "two"
    },
    {
        name: "Three",
        href: "three",
    },
    {
        name: "Four",
        href: "four",
    },
    {
        name: "Five",
        href: "five",
    },
];

export async function seed(db: DB) {
    await db.insert(shopNavLinks).values(links);
}
*/
